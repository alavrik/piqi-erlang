#!/bin/sh

dir=$(cd -P -- "$(dirname -- "$0")" && pwd -P)
PIQI=`ERL_CRASH_DUMP=""  erl -noshell -kernel error_logger silent -pa ${dir}/../../piqic-erlang/ebin -eval 'io:format("~s", [piqi:find_piqi()]), halt().'`

"$PIQI" $@
