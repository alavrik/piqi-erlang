% we need this to be able to -include_lib("piqi/include/piqirun.hrl").
{lib_dirs, ["../.."]}.


{escript_name, "piqic-erlang"}.
{escript_emu_args, "%%! -escript main piqic_erlang\n"}.


{post_hooks, [
    {compile, "rebar escriptize"}
]}.


% ex: sw=4 ts=4 et ft=erlang
